/**
 * Contrato esperado para implementacoes de RelatoriosRepository.
 *
 * @typedef {Object} IRelatoriosRepository
 * @property {() => Promise<Array<Object>>} contarEntregasPorStatus
 * @property {() => Promise<Array<Object>>} listarMotoristasAtivosComEntregasAbertas
 */
export {};
